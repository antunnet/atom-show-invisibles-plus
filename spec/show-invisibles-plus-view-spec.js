'use babel';

import ShowInvisiblesPlusView from '../lib/show-invisibles-plus-view';

describe('ShowInvisiblesPlusView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
